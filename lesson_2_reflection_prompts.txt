What happens when you initialize a repository? Why do you need to do it?

After the initialization, it creates .git hidden holder which helps to track changes. 

How is the staging area different from the working directory and the repository?

Staging area --> It is area where one can add files that need to be committed
Working directory --> It is current working directory. 
 
What value do you think it offers?

Staging area offers to have certain files in it for commitment.

How can you use the staging area to make sure you have one commit per logical change?

By comparing working directory and staging area, we can make commit/logical change.Staging area helps to modify the numbers of file we want to commit.  

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

Branches are used to create different version of project which helps to introduce experimental features without affecting current branch. 

How do the diagrams help you visualize the branch structure?

Diagrams helps to find current branch and commit, will make easier to navigate. 

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

Git merge will merge the changes into single branch. 
Diagrams is the easiet way to understand the trees. 

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Git will not be able to merge automatically. it has to be done manually. 